version: "3"
services:
  web:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile.txt
    ports:
      - '8099:80'
    container_name: app-cegah-terbaru
    env_file: .env.example